[project]
name = "jellyfin-playlist-backup"
version = "1.0.0"
description = "Backs up and restores Jellyfin playlists."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.5",
    "requests>=2.34.2",
]

[project.scripts]
jfpl = "jellyfin_playlist_backup.__main__:main"